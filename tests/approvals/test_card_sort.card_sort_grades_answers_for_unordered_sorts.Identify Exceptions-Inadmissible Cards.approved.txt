[Attempt(question='Identify Exceptions',
         username='foo123',
         course='CS1',
         semester='SX',
         idx=0,
         attempt={'Cards': [],
                  'IndexError': ['def remove_evens(vals):\n    for i in range(len(vals) - 1, -1, -1):\n        num = vals[i]\n        if num % 2 == 0:\n            vals.pop(num)',
                                 'def pairwise_sums(numbers):\n    result = []\n    for i in range(len(numbers)):\n        pair = numbers[i] + numbers[i + 1]\n        result.append(pair)\n    return result'],
                  'TypeError': ['FOO BAR', 'def double_tuple(num_tuple):\n    sorted_nums = sorted(num_tuple)\n    return num_tuple + sorted_nums'],
                  'ValueError': ['def min_divisible_by_3(nums):\n    nums = [e for e in nums if e % 3 == 0]\n    return min(nums)', 'def sum_up_to_n():\n    n = int(input("n: "))\n    return n * (n + 1) / 2'],
                  'ZeroDivisionError': ['def mean_of_evens(nums):\n    nums = [e for e in nums if e % 2 == 0]\n    return sum(nums) / len(nums)',
                                        'def remaining_slices(n_slices, n_people):\n    return n_people % n_slices',
                                        'def vowel_ratio(word):\n    word = word.lower()\n    vowels = 0\n    for vowel in "aeiou":\n        vowels += word.count(vowel)\n    return vowels / len(word)']},
         grade=0.8333333333333334,
         is_admissible=False,
         is_genuine=True,
         extra_data={'all_moved': True})]
